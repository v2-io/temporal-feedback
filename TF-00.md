# TF-00: Notation and Conventions

This document defines all symbols used in Temporal Feedback Theory, serving as a single authoritative reference. When a symbol appears in any TF document, its meaning is as defined here unless explicitly noted otherwise.

## Primitives (TF-01)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\Omega$ | State space | Environment state (unobservable totality) | TF-01 |
| $\Omega_t$ | State | Environment state at time $t$ | TF-01 |
| $\mathcal{O}$ | Set | Observation space | TF-01 |
| $\mathcal{A}$ | Set | Action space | TF-01 |
| $o_t$ | $\in \mathcal{O}$ | Observation at time $t$ | TF-01 |
| $a_t$ | $\in \mathcal{A}$ | Action at time $t$ | TF-01 |
| $h$ | Function | Observation function: $o_t = h(\Omega_t, \varepsilon_t)$ | TF-01 |
| $T$ | Distribution | Transition: $\Omega_{t+1} \sim T(\cdot \mid \Omega_t, a_t)$ | TF-01 |
| $\varepsilon_t$ | Random variable | Observation noise | TF-01 |
| $H(\cdot)$ | Functional | Shannon entropy | Standard |
| $I(\cdot; \cdot)$ | Functional | Mutual information | Standard |

## Temporal Structure (TF-01b)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\mathcal{H}_t$ | Sequence | Interaction history: $(o_1, a_1, o_2, a_2, \ldots, a_{t-1}, o_t)$ | TF-01b / TF-02 |
| $do(\cdot)$ | Operator | Pearl's intervention operator (Level 2) | TF-01b |
| $\text{CIY}(a)$ | Scalar $\geq 0$ | Causal information yield of action $a$ | TF-01b |

## The Model (TF-02)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $M_t$ | $\in \mathcal{M}$ | Model state at time $t$ | TF-02 |
| $\mathcal{M}$ | Set | Model space (the class of representable models) | TF-02 |
| $\phi$ | Function | Compression: $M_t = \phi(\mathcal{H}_t)$ | TF-02 |
| $f$ | Function | Recursive update: $M_t = f(M_{t-1}, o_t, a_{t-1})$ | TF-02 |
| $S(M_t)$ | $\in [0, 1]$ | Model sufficiency (sufficient statistic quality) | TF-02 |
| $\mathcal{F}(\mathcal{M})$ | $\in [0, 1]$ | Model class fitness: $\sup_{M \in \mathcal{M}} S(M)$ | TF-02 |
| $\beta$ | Scalar $> 0$ | Information bottleneck trade-off parameter | TF-02 |

## Event-Driven Dynamics (TF-03)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $e$ | Event | An atomic observation or action-completion event | TF-03 |
| $\tau$ | $\in \mathbb{R}_{\geq 0}$ | Continuous timestamp of an event | TF-03 |
| $\mathcal{E}$ | Sequence | Event stream: $\{(e_i, \tau_i)\}$ | TF-03 |
| $\nu^{(k)}$ | Rate (Hz) | Event rate on channel $k$ | TF-03 |
| $M_{\tau^-}$, $M_{\tau^+}$ | $\in \mathcal{M}$ | Model state just before / after event at $\tau$ | TF-03 |
| $U_o^{(k)}$ | Scalar $> 0$ | Observation uncertainty on channel $k$ | TF-03 |
| $\nu_{\text{eff}}$ | Rate ($t^{-1}$) | Effective adaptation rate: $\sum_k \nu^{(k)} \cdot \eta^{(k)*}$. Identical to $\mathcal{T}$ (TF-08). | TF-03 |

## Mismatch Signal (TF-04)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\hat{o}_t$ | $\in \mathcal{O}$ | Predicted observation: $\mathbb{E}[o_t \mid M_{t-1}, a_{t-1}]$ | TF-04 |
| $\delta_t$ | $\in \mathcal{O}$ | Mismatch signal: $o_t - \hat{o}_t$ | TF-04 |

## Update Gain (TF-05)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\eta$ | Scalar, vector, or matrix | Update gain (general) | TF-05 |
| $\eta^*$ | Same | Optimal update gain | TF-05 |
| $\eta^{(k)*}$ | Same | Optimal gain on channel $k$ | TF-05 |
| $U_M$ | Scalar $> 0$ | Model uncertainty about the observed quantity | TF-05 |
| $U_o$ | Scalar $> 0$ | Observation channel uncertainty | TF-05 |
| $g$ | Function | Mismatch transform: maps $\delta_t$ to update direction | TF-05 |

## Action Selection (TF-06)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\pi$ | Function or distribution | Policy: $a_t = \pi(M_t)$ or $a_t \sim \pi(\cdot \mid M_t)$ | TF-06 |
| Action fluency | Qualitative property | Degree to which effective action flows from $M_t$ without deliberation. Distinct from model sufficiency $S(M_t)$. | TF-06 |
| $\lambda(M_t)$ | Scalar $\geq 0$ | Exploration-exploitation balance weight | TF-06 |

## Deliberation Cost (TF-06.5)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\Delta\tau$ | Duration $\geq 0$ | Deliberation duration | TF-06.5 |
| $\Delta\eta^*(\Delta\tau)$ | Scalar $\geq 0$ | Gain improvement from deliberation of duration $\Delta\tau$ | TF-06.5 |

## Structural Adaptation (TF-07)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\Phi$ | Function | Structural update: $\mathcal{M}_{t+1} = \Phi(\mathcal{M}_t, \text{performance history})$ | TF-07 |

## Adaptive Tempo and Dynamics (TF-08)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $\mathcal{T}$ | Rate (surprise/time) | Adaptive tempo: $\sum_k \nu^{(k)} \cdot \eta^{(k)*}$ | TF-08 |
| $\rho(t)$ | Rate (surprise/time) | Environment change rate (rate of new mismatch introduction) | TF-08 |
| $\lvert\delta\rvert_{ss}$ | Scalar $\geq 0$ | Steady-state mismatch: $\rho / \mathcal{T}$ | TF-08 |

## Lyapunov Analysis (Appendix A)

| Symbol | Type | Meaning | Defined in |
|--------|------|---------|------------|
| $F(\mathcal{T}, \delta)$ | Function | Correction function (general nonlinear) | App. A |
| $w(t)$ | Vector | Disturbance (new mismatch), $\|w(t)\| \leq \rho$ | App. A |
| $\alpha$ | Scalar $> 0$ | Lower sector bound of correction function | App. A |
| $R$ | Scalar $> 0$ | Radius of sector-condition region (model class capacity) | App. A |
| $R^*$ | Scalar $> 0$ | Ultimately bounded mismatch radius: $\rho/\alpha$ | App. A |
| $\Delta\rho^*$ | Scalar $\geq 0$ | Adaptive reserve: $\alpha R - \rho$ | App. A |
| $\gamma_A$ | Scalar $> 0$ | Coupling effectiveness of $A$'s actions on $B$'s disturbance | App. A |
| $V(\delta)$ | Scalar $\geq 0$ | Lyapunov function: $\frac{1}{2}\|\delta\|^2$ | App. A |

## Conventions

**Subscript $t$**: Discrete time index. Used when events arrive uniformly on a single channel.

**Subscript $\tau$**: Continuous time. Used in the event-driven formulation (TF-03).

**Superscript $(k)$**: Channel index. Distinguishes observation or action channels with distinct rates and noise characteristics.

**Calligraphic letters** ($\mathcal{M}$, $\mathcal{O}$, $\mathcal{A}$, $\mathcal{E}$, $\mathcal{H}$, $\mathcal{T}$): Sets, spaces, or aggregate quantities. Exception: $\mathcal{T}$ is a scalar rate, not a set — the calligraphic is used to distinguish adaptive tempo from temperature or other common uses of $T$.

**$\lvert \cdot \rvert$**: Norm of a vector (for mismatch magnitude) or cardinality of a set, determined by context.

## Units (Informal)

The theory is stated in natural (dimensionless information-theoretic) units where possible:

- $\eta^*$: Dimensionless ratio in $[0, 1]$
- $\nu^{(k)}$: Events per unit time (Hz)
- $\mathcal{T}$: Inverse time ($t^{-1}$). Informally: "effective correction rate."
- $\rho$: Mismatch per unit time ([surprise] $\cdot t^{-1}$). Rate at which new mismatch is introduced.
- $S(M_t)$: Dimensionless ratio in $[0, 1]$

**Dimensional analysis of the mismatch ODE.** In $d\lvert\delta\rvert/dt = -\mathcal{T}\lvert\delta\rvert + \rho$: the LHS has units [surprise $\cdot t^{-1}$]; the term $\mathcal{T}\lvert\delta\rvert$ has units $[t^{-1}] \cdot [\text{surprise}] = [\text{surprise} \cdot t^{-1}]$; and $\rho$ has units $[\text{surprise} \cdot t^{-1}]$. All terms are consistent. Note that $\mathcal{T}$ and $\rho$ have *different* units — the shorthand "$\mathcal{T} > \rho$" used in TF-08 is the normalized form where $|\delta|$ is measured in units of the critical mismatch threshold $|\delta_{\text{critical}}|$ (making $|\delta|$ dimensionless). The full persistence condition is $\mathcal{T} > \rho / |\delta_{\text{critical}}|$, which is dimensionally consistent.
